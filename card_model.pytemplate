'''This module is autogenerated! Do not edit directly'''
import utils

_cards = utils.load_cards('{{ d }}')

# "enum-like" namespace for easy access via tab completion
class Cards:
    {% for card in cards.root -%}
    {{ card.name.upper().replace(' ', '_').replace('.','') }} = _cards.get('{{ card.name }}')
    {% endfor %}
